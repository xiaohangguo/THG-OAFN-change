# 项目阶段总览与执行摘要

最后更新：2026-09-20（云机冲刺线收官）
状态：**论文主表定稿：XGBoost 0.6214±0.0040（5种子）；GNN 三形态终审关闭（0.0125）；SHAP 78列归因完成；冲刺线 100%**

## 已确认的研究决策

| 决策 | 结论 | 原因 |
|---|---|---|
| 金融任务 | 反洗钱交易风险监测 | 与金融硕士论文、交易图和时间因果约束一致 |
| 主数据 | IBM AML HI-Small | 有交易时间、账户关系、支付属性和洗钱标签；可复现 |
| 外部验证 | Elliptic++ | 真实链上金融交易图，用于检验跨金融场景泛化 |
| 主模型 | CTH-RiskGNN | 因果时间窗口、关系感知异构聚合、成本敏感风险排序 |
| 归因机制 | 架构内建三层归因 | 开题承诺欺诈识别给出归因；旧 TriExplainer 属事后装饰已弃用。新架构在关系级注意力、因果窗口贡献、反事实边删除三层输出可验证归因（fidelity/sparsity 指标验收），归因接口与模型同步实现，不事后追加 |
| 不平衡处理 | 训练集权重/Focal Loss与阈值校准 | 不生成会污染时序拓扑的虚假交易或节点 |
| 主指标 | AUPRC、Recall@K、Precision@K、成本敏感损失 | 符合极端不平衡下的告警审核业务 |

## 阶段计划

| 阶段 | 内容 | 交付物 | 完成标准 |
|---|---|---|---|
| Phase 1 | 数据治理、存储规范、数据审计、旧主线清退 | 数据契约、审计器、配置、清退日志 | 能对原始 CSV 分块审计并生成不可变元数据 |
| Phase 2 | IBM 数据接入、时间切分、无未来泄漏的特征与表格基线 | 处理清单、LightGBM/MLP 基线预测 | 每个特征有时间可见性证明，保存原始预测 |
| Phase 3 | 静态历史异构图基线 | 账户—交易—银行图、GraphSAGE/HGT 结果 | 图方法与表格方法在同一切分上可比较 |
| Phase 4 | 因果时序异构图模型 CTH-RiskGNN（含归因接口） | 时间编码、因果邻居采样、注意力归因导出、消融 | 所有消息仅来自过去；注意力权重可导出至案例级；消融验证各模块 |
| Phase 5 | 稳健性、金融告警评价、Elliptic++ 外部验证 | 5 seed 结果、成本曲线、外部结果 | 置信区间、固定阈值、外部数据协议齐全 |
| Phase 6 | 归因验证、论文实验表、案例解释、复现实验包 | fidelity/sparsity 验证、反事实案例、图表、数据卡、环境锁定、复现命令 | 归因删除 top-k 边/窗口后分数显著变化且报告 fidelity；独立机器按文档可重跑 |

## Phase 1 本次执行内容

1. 创建 D 盘本地工作区，仅保存运行元数据、缓存与结果，不把大数据推送 GitHub。
2. 固化 IBM AML HI-Small 的字段契约、时间切分与指标配置。
3. 实现分块 CSV 审计器，输出行数、正样本率、时间范围、字段映射和文件指纹。
4. 删除 Amazon 评论图、伪时序模型、无效实验结果与基于这些结果生成的图表。
5. 运行审计器单元测试和代码静态检查，提交并推送一个 Phase 1 版本。

## 结果解释规则

不对新数据承诺特定 F1。一个结果仅在以下条件同时满足时才可写入论文：时间切分固定、特征与图无未来泄漏、阈值由验证集锁定、至少 5 个模型种子、保存逐笔预测和配置快照。

## 更新日志

### 2026-09-17 — Phase 1A 完成

- 研究主线从 Amazon 评论用户图转为金融反洗钱时序交易图。
- 已创建 D 盘本地工作区、数据治理规范、实验协议、字段契约、配置和分块 CSV 审计器。
- 审计器与字段契约通过 5 项自动化测试；尚未用真实金融数据训练或生成指标。
- Amazon 数据、伪时序模型、旧结果、旧图表和旧汇总文件已从现行分支删除；具体原因和替代方案见 `docs/DEPRECATION_LOG.md`。
- **阶段问题：** 本机尚无 Kaggle 凭据、Kaggle CLI 或下载的数据文件，故不能执行真实 IBM 文件审计。
- **解决建议：** 用户登录 Kaggle 接受数据条款后配置 API token，或手动下载两份 HI-Small CSV 至 D 盘。详见 `docs/DOWNLOAD_IBM_AML.md`；下载完成后先运行审计器，审计通过才进入 Phase 2。

### 2026-09-19 — Phase 2 结构特征突破：XGBoost AUPRC 0.412 → 0.489

在原始 33 个账户级因果特征之上，新增两类**严格无泄漏的图结构信号**（朴素表格近似），XGBoost 全 5 种子测试集 AUPRC 从 **0.412 提升至 0.4888**（+18.6%），所有主指标全面超越。

| 指标 | 原基线 (33特征) | 新 (60特征) | 提升 |
|---|---|---|---|
| AUPRC | 0.412±0.002 | **0.4888±0.0035** | +18.6% |
| ROC-AUC | 0.982 | 0.9866 | +0.5pt |
| Recall@0.1% | 0.336 | 0.3754 | +3.9pt |
| Precision@0.1% | 0.596 | 0.6646 | +6.9pt |
| Recall@1% | 0.679 | 0.7841 | +10.5pt |

**新增特征的设计洞察**（两次失败后迭代得出）：

1. **账户对交互强度（pair）**：`(源,目标)`账户对的 1h/24h/7d 历史交互次数与金额，严格过去窗口。洗钱账户对仅交互 1 次时洗钱率 0.83%，而重复交互账户对仅 0.077%（**10 倍差距**）——"账户对新颖度"是图结构的本质信号。
2. **实体兄弟账户差分（entity）**：账户 CSV 的 `Entity ID`→ 实体级历史，再与账户自身历史**差分**，得出"同实体其他账户活动"。对单账户实体自动归零（不添噪），对多账户实体暴露分层/整合信号。
3. **自环标志**：`is_self_loop`（自环洗钱率仅 2e-05）。

**关键方法学教训**（写入经验，供后续阶段复用）：
- 朴素实体聚合（直接用实体总量）**有害**（62% 实体单账户=纯冗余噪声，AUPRC 0.0863→0.0783），必须用差分形态去耦。
- pair 与 entity 是**互补**信号：单独加 entity 全量无效（0.412→0.375），叠加 pair 后全量**大幅转正**（→0.489）。结构特征需组合表达多维拓扑，单维注入可能被噪声淹没。
- 中规模样本（前 200 万行）评估有误导性：entity 在中规模看似 +31%，但只有与 pair 组合才能在全量稳定复现。**评估必须用完整时序。**

这些特征是 Phase 3 图模型的廉价先行验证——它们证明了账户-实体拓扑含增量信息，且无泄漏表达可行。Phase 3 真图模型应超越此 0.489 门槛。

### 2026-09-20 — 云机冲刺线收官（RTX 5090，全链路当晚完成）

**跨机器一致性验证**（数据 SHA-256 一致、特征矩阵 78/78 列 md5 一致、AUPRC 0.5908 vs 本地 0.5846 平台差 0.006），途中捕获两个环境级大坑并修复：pandas 3 默认微秒分辨率静默腐蚀全部窗口特征（代码钉死 `datetime64[ns]` + requirements 锁 pandas<3）；xgboost GPU hist 与 CPU hist 是不同算法（XGB 钉 CPU）。

**论文主表定稿（78 列同口径，时序切分，5 种子）**：

| 模型 | test AUPRC | 备注 |
|---|---|---|
| **XGBoost regularized** | **0.6214 ± 0.0040** | 超参搜索 5 候选胜出（min_child_weight=50, reg_lambda=5, colsample=0.7），搜索记录落盘 |
| MLP | 0.2803 | 画像列使其从 0.075 升至 0.280（图统计对神经网帮助大于树模型） |
| LightGBM | 0.2619 | 同口径对照 |
| GATv2 + 20 维强行为画像 | 0.0125 | 文献形态终审，见下 |

**GNN 路线终审（三形态全部测完，正式关闭）**：SAGE 均值（0.030）→ GATv2 注意力+完整训练循环（0.012）→ GATv2 + 20 维强行为节点画像（0.0125）。强节点特征几乎不改变结局，死因复合：评分头强特征梯度跛行、低同质性稀释、边分类嵌入瓶颈。结论：图信息有价值（探针 +0.098 已通过画像列注入 GBDT 兑现），但端到端 GNN 在严格无泄漏协议下不可复现文献声称的优势——与 NeurIPS 2022 表格数据 GBDT 统治结论一致，作为诚实负结果写入论文。

**SHAP 78 列终版归因**（冠军模型）：top-3 = pair_w7d_count（账户对 7 天频次）、payment_format、src_w1h_count（突发高频）；画像列（不同对手数）进入 top12。fidelity：遮蔽 top-10 后被标记交易均分 0.923 → 0.270（降 70.7%；78 列特征冗余高于 60 列版，单遮 top-k 的坍缩弱化符合统计直觉，论文如实报告）。

**成本**：云机全晚约 ¥20。全部结果已拉回本地 `cloud_archive/`（23 个文件）。

### 2026-09-20 — 图路径 bug 修复、探针反转与混合架构新王者

**诚实记录三次战役**：(1) 发现 BandData localize 空 dict bug——`--no-graph` 消融与完整版逐位一致暴露图路径从未生效，此前 0.076 实为纯 MLP；已修复并加 seen 率防回归断言（mean 0.969）。(2) 修复后 GraphSAGE 首战 test AUPRC 0.030——均值聚合在低同质性欺诈图上有害，符合欺诈图文献已知失效模式，如实入档。(3) **探针实验反转全局**：快照画像 18 列（GNN 的输入）直接拼给 XGBoost，AUPRC 0.487 → **0.585**（+0.098，相对 +20%），判决"图信息有手工特征之外的增量，败因在聚合架构而非图本身"。

**新王者**：XGBoost + 60 手工因果特征 + 18 快照画像 = 0.585（seed 42，5 种子验证中）。画像列已提升为正式模块 `extract_profile_columns`（严格过去 7 天窗口、12h 带、双侧 8 维画像 + 2 seen 位）。

**归因主线首份硬证据**（XGBoost+SHAP，60 列版）：top-10 特征全部金融语义明确（账户对 7 天频次居首、支付方式、突发高频、跨行计数）；fidelity 验证——遮蔽 top-10 SHAP 特征后被标记交易均分 0.800 → 0.0000177（99.998% 坍缩），归因非装饰；40 案例级分解已存（20 TP + 20 FP）。

**研究问题重构定稿**：开题"精准性与可解释性无法兼顾"深化为 Rudin (2019) 立场下的双 RQ——RQ1 无泄漏协议下的检测精度（门槛 0.585），RQ2 架构内建归因的 fidelity/sparsity 验证与 SAR 式案例解释。图模型若最终不敌混合架构，论文主线为"GBDT 精度 + GNN 关系归因"的混合叙事，两贡献均完整。

**下一步**：5 种子锁定 0.585 方差；GATv2 注意力版（低同质性正解 + 注意力导出即归因接口）；78 列新特征集重跑 SHAP；跨带记忆（CTH-RiskGNN 核心）。

### 2026-09-19 — Phase 2 多模型基线完成

33 个严格因果特征（新增双侧账户严格历史度数，对 fan-in/fan-out 洗钱形态敏感）单次构建、三模型共享；时间切分 60/20/20；5 种子完整协议；验证集仅用于早停，只报告测试集。

| 模型 | AUPRC | ROC-AUC | P@0.1% | R@0.1% | R@1% |
|---|---|---|---|---|---|
| XGBoost | 0.412±0.002 | 0.982 | 0.596 | 0.336 | 0.679 |
| LightGBM | 0.161±0.016 | 0.925 | 0.318 | 0.180 | 0.498 |
| MLP (GPU) | 0.075±0.047 | 0.945 | 0.153 | 0.086 | 0.258 |

判读：(1) XGBoost AUPRC 0.412 为随机水位 230 倍，P@0.1%=0.60 具备告警排序实用价值，是 Phase 3/4 图模型必须正面击败的门槛；(2) MLP 种子崩溃（两个种子塌至 0.019）为极端不平衡下朴素网络的典型行为，反向支撑关系结构建模动机；(3) 逐种子测试集预测已按协议落盘 `results/phase2/`。

附带修正：首版 LightGBM `best_iteration=2` 问题确认为 scale_pos_weight=660 过激，改为 25 + 学习率 0.03 + 3000 轮 AUPRC 早停后 AUPRC 从 0.026 升至 0.161。

### 2026-09-19 — Phase 1 闭环 + Phase 2 首基线

**Phase 1 完成**：Kaggle bundle v12411329 整包下载（8,176,169,418 字节，zip 级版本指纹），提取 HI-Small 三文件至 D 盘。审计通过：5,078,345 笔交易 / 正样本 5,177（0.102%）/ 时间跨度 2022-09-01 至 09-18 / 账户 515,080 / 银行 30,470 / 支付方式 7 种 / 无效时间戳 0。逐文件 SHA-256 已写入 `metadata/ibm_hi_small_audit.json`，bundle 成员清单见 `metadata/bundle_manifest.json`。数据许可 CDLA-Sharing-1.0、合成性质将在论文数据卡披露。

**Phase 2 首个基线（LightGBM + 31 因果特征，时间切分 60/20/20）**：

| 指标 | 验证集 | 测试集（时序外推） |
|---|---|---|
| ROC-AUC | 0.825 | 0.873 |
| AUPRC | 0.017 | 0.026（随机水位 0.0018 的 14.5 倍） |
| Recall@0.1% | 0.362 | 0.287 |
| Precision@0.1% | 0.385 | 0.507 |
| Recall@1% | 0.472 | 0.381 |

阶段判读：这是无未来泄漏、严格时序外推、千分之一不平衡下的真实起点，与旧项目泄漏态 F1=0.90 不可比。测试段正样本密度（0.177%）高于训练段（0.075%），分布漂移由时序切分如实暴露。**已知问题**：`best_iteration=2`，scale_pos_weight≈660 梯度过激触发秒停，LightGBM 未收敛，下一版修训练配置（降低权重/学习率，延长轮次）后该基线应显著上移。

**环境事件**：下载期晚高峰国际出口拥塞，实测 44 节点全部 0.02-0.46MB/s，切换德国优化节点后完成收尾；Verge 外部控制已启用便于后续自动切换。

### 2026-09-19 — 归因缺口修正与环境准备

- 识别出计划缺口：开题承诺"欺诈识别给出归因"，但 Phase 1 版本的计划中归因仅在 Phase 6 出现"案例解释"一词，架构决策表无归因位置，与旧项目 TriExplainer 的教训（事后装饰性解释、案例概率含随机数）同构。已将归因升级为架构内建决策：关系级注意力归因（HGT 节点级注意力，非全局标量）、因果窗口贡献分解、反事实边删除，Phase 4 实现接口，Phase 6 用 fidelity/sparsity 验收。
- 新增 `scripts/download_ibm_aml.py`：kagglehub 白名单下载 HI-Small 三文件至 D 盘，杜绝误下 42 GB Large 包。
- 环境：本机 Python 3.13.1 唯一版本，`requires-python` 放宽至 `<3.14`；kagglehub 已安装；D 盘工作区目录已创建。仍缺 Kaggle 凭据。
