# 实验报告：反洗钱交易风险检测（Phase 2-3 冲刺线）

生成：2026-09-20 · 数据：IBM AML HI-Small（SHA-256 已锁定）· 全部结果可由仓库脚本复现

## 1. 实验协议

| 项 | 设定 |
|---|---|
| 切分 | 按交易时间 60/20/20，边界吸附完整时间戳，同刻交易永不跨集合 |
| 防泄漏 | 全部特征严格 `timestamp < t` 可见；同刻互不可见；scaler/早停仅用训练/验证集 |
| 种子 | 主表 5 种子（42/123/456/789/2024）报告 mean±std；探索单种子 |
| 早停 | 验证集 AUPRC，树模型 patience 200 轮，神经网 patience 5-8 epoch |
| 指标 | 主：AUPRC；辅：ROC-AUC、Precision@K、Recall@K（K=0.1%/1% 审核容量） |

正样本率 0.102%（5,177/5,078,345）→ 随机基线 AUPRC = 0.0018。

## 2. 主对比表（78 列同口径特征，测试集 = 时序外推）

| 模型 | AUPRC | ROC-AUC | Precision@0.1% | Recall@0.1% | Recall@1% | 种子数 |
|---|---|---|---|---|---|---|
| **XGBoost（regularized）** | **0.6214 ± 0.0038** | **0.9904** | **0.8337** | 0.4709 | **0.8427** | 5 |
| MLP | 0.2803 | — | — | — | — | 1 |
| LightGBM | 0.2619 | — | — | — | — | 1 |
| 随机基线 | 0.0018 | 0.5 | 0.001 | 0.001 | 0.01 | — |

业务读数：对每日 500 万笔交易，只审核风险最高的 0.1%（约 1,000 笔）可捕获 47.1% 洗钱且 83.4% 命中；放宽至 1% 审核量捕获 84.3%。AUPRC 0.6214 = 随机基线的 345 倍。

## 3. 特征消融（XGBoost，seed 42，同一切分）

| 特征集 | 列数 | AUPRC | 增量 |
|---|---|---|---|
| 基础因果窗口（账户双侧 1h/24h/7d） | 33 | 0.4119 | — |
| + 账户对频次 / 实体差分 | 60 | 0.4888 | +0.077 |
| + 快照画像 18 列（严格过去 7 天窗口） | 78 | 0.5846 | +0.096 |
| + 超参优化（regularized） | 78 | 0.6276 | +0.043 |

探针实验证实画像列承载手工特征之外的图结构信息（判决 "adds signal beyond handcrafted features"）。

## 4. 超参搜索（5 候选，验证集选优，记录存档 `hparam_search/`）

| 候选 | 关键参数 | AUPRC | 轮数 |
|---|---|---|---|
| baseline | depth 8, mcw 1 | 0.5908 | 2676 |
| deeper | depth 10 | 0.5979 | 1562 |
| shallower-lr | lr 0.03 | 0.5901 | 2906 |
| **regularized** | **mcw 50, λ=5, colsample 0.7** | **0.6276** | **433** |
| fast-wide | lr 0.08, depth 6 | 0.6077 | 296 |

## 5. GNN 端到端路线：七发诊断链（全部 seed 42）

| # | 配置 | AUPRC（test） | 隔离变量 |
|---|---|---|---|
| D1 | 无图对照（同训练循环） | 0.0885 | 训练循环基线 |
| 1 | GraphSAGE + 8 维画像 | 0.0297 | 均值聚合 |
| 2 | GATv2 注意力（完整训练循环） | 0.0120 | 聚合方式 |
| 3 | GATv2 + 20 维强行为画像 | 0.0125 | 节点特征强度（文献形态） |
| D5 | 评分头 warmup（eval 模式 bug 版） | 0.0215 | 调度（污染） |
| D6 | 干净 warmup | val 0.1502 / 0.0029 | 图加入后 val 即崩 |
| D7 | 冻结嵌入注入 XGBoost | 0.5922（vs 0.6276，**-0.035**） | 嵌入信息量 = 噪声 |

结论：图嵌入的三个出口（端到端注入、warmup 调度、冻结注入）被系统性排除；监督梯度弱到训不出超越初始化的表示；噪声列还通过 `colsample` 采样稀释伤害树模型。**图信息的正确出口是因果画像特征（+0.096，第 3 节）**。与表格任务上 GBDT 统治的文献结论（Grinsztajn et al., NeurIPS 2022）一致。

## 6. 归因（SHAP，冠军模型，78 列）

Top-5 特征：`pair_w7d_count`（账户对 7 天频次，1.29）· `payment_format`（1.27）· `src_w1h_count`（突发高频，0.63）· `src_w24h_count`（0.39）· `src_w7d_count`（0.37）。全部具备 AML 业务语义；画像列（不同对手数）进入 Top-12。

Fidelity：遮蔽 Top-10 SHAP 特征后，被标记交易均分 0.923 → 0.270（坍缩 70.7%）。78 列特征冗余度高于稀疏特征集，坍缩弱于 60 列版的 99.998% 属统计预期，如实报告。40 个案例级归因（20 TP + 20 FP）存于 `attribution/`。

## 7. 环境与复现

- 本地：i5-12400 / RTX 3060 / Python 3.13 / pandas 2.3 / xgboost 3.4（CPU）
- 云机：RTX 5090 32G / torch 2.8+cu128 / 同 xgboost
- 跨机一致性：数据 SHA-256 一致、特征矩阵 78/78 列 MD5 一致、AUPRC 平台差 0.006
- 环境坑记录：pandas 3 默认微秒分辨率静默腐蚀窗口特征（已钉 `datetime64[ns]`）；xgboost GPU hist 与 CPU hist 算法不同（已钉 CPU）
- 每个数字的 metrics JSON 在 `experiments/results/`；逐笔预测 parquet 按数据治理约定仅存本地

## 8. 论文定位

主线：**无泄漏协议 + 因果图统计特征 + GBDT**（RQ1：0.6214±0.0040）；**架构外可验证归因**（RQ2：SHAP + fidelity）。GNN 七发负结果作为系统性失败分析章节，支撑"图信息应以因果聚合特征而非端到端嵌入进入模型"的方法论结论。
